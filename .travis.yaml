after_script:
  - docker images

before_script:
  - IMAGE="ethereum:${VERSION/\//-}"

env:
  - VERSION=2.7.2

language: bash

script:
  - docker build -t "$IMAGE" .
  - docker run "$IMAGE" version | grep -i "parity"

services: docker
